{
  "name": "koober-interview",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "engines" : { 
    "node" : ">=8.0.0" 
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:integration": "node index | diff expected_output.json - && echo 'Test passed!'"
  }
}
